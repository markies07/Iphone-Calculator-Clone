<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the button is pressed -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFC15B" /> <!-- Lighter orange when pressed -->
            <corners android:radius="500dp" /> <!-- Rounded corners -->
        </shape>
    </item>
    <!-- Default state (when the button is not pressed) -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#FFA500" /> <!-- Default orange color -->
            <corners android:radius="500dp" />
        </shape>
    </item>
</selector>
